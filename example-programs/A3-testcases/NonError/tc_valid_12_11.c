// Checks for combination of arithmetic, logical, relational and ternary operators
void main(){
    int a,b,c;
    bool p,q,r;
    b = ((a + b) > (c - a)) ? (q || r && p) ? (a /b + c) : (--a) : (b--a);
}