// bool c;
int a,b,c;

// void main;
void main(int b);

int blah() {
    // c = (a + b + c);
    return (a + b + c);
}


void main (int a)
{
    // void alpha;
    // void main;
    bool temp0, d,e;
    // int d;
    string z;

    print "Enter value of a: ";
    read a;
    print "Enter value of b: "; 
    read b;

    d = e;

    a = -------------b;

    c = a+a + b-b;
    e = b*b > a/a;

    d = !(a > b);
    c = a + b + -c;
    c = -b * a;
    c = a * -c * b;
    d = (a >= b);

    z = "input";


    a = d ? 3 : 2;

    temp0 = (d && e) || (d || e);

    // z = (d && e) ? "a" : e ? "b" : "none";
    print b;
    // return 3;
}