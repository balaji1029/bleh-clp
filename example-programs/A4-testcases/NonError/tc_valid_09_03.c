// Program with nested if-else statement with complex boolean expression

void main() {
    int a;
    bool b;
    a = 23213;
    if ((b && (4 > 3)) || !(5 < 6) && (a == 433) || (b?b:!b)) {
        print a;
        if ((a > a * 99 ? b : b && !b)) {
            a = a + 534;
            print a;
        }
    } else {
        if ((a > a * 99 ? b : b && !b)) {
            a = a + 534;
            print a;
        } else if ((a > a * 99 ? b : b && !b)) {
            a = a + 534;
            print a;
        } else {
            if ((a > a * 99 ? b : b && !b)) {
                a = a + 534;
                print a;
            } else {
                if ((a > a * 99 ? b : b && !b)) {
                    a = a + 534;
                    print a;
                } else if ((a > a * 99 ? b : b && !b)) {
                a = a + 534;
                print a;
            }
                }
        }
    }
}