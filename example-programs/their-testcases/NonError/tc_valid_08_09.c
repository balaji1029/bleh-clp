// Checks whether complicated expressions with braces are handled
void main()
{
    alpha = ((a + (b * (c - d))) / ((x-y) + (foo * (bar + 3)))) + (("abc" + "") * ((p / q) - (r + (s * t)))) * (-(-(("final_boss"))));
}