// Checks for combination of arithmetic, logical, and relational operators along with if-else statements
void h(){
    float x, y, z;
    if(x == y) x  = z;
    else print y;
}

int f(int a, int b){
    int x;
    x = 0;
    while(x == 0){
        a = a + b;
    }
    return a;
}
void main(){
    int a,b,c;
    float x,y;

    if(x < 7.0 && a > 40){
        h();
    }
    else if(b + c >= 81 && x + y < 5.6 && a > 20){
        a = f(b, c);
    }
    else {
        while(a == b){
        print "Good morning";
        a = f(b, c);
        }
    }
}