// Complex expressions in if-else statements

int c_to_f(int c) {
    return (c / 5) * 9 + 32;
}

int f_to_c(int f) {
    return (f - 32) / 9 * 5;
}

void main() {
    int val;
    read val;
    val = c_to_f(val);
    val = f_to_c(val);
    if (val > 102 ? !(1==1) : !!!!!!!!!(0!=0)) {
        print "bad fever";
    } else if (((val > 100) ? (2323 * val / 3234 + 233 + ------3233) : (4)) == 0 || (1==1)) {
        print "fever";
    } else if (val > ---------------102 && (0!=0)) {
        print "maybe fever";
    } else {
        print "no fever";
    }
}