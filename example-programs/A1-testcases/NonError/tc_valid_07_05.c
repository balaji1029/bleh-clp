// Checks arithematic expressions without braces

void main(){
    int a, b, c, d, e, f, g, h, i, j;
    float p, q, r, s, t, u, v, w, x, y;

    a = e - g * h + i / -j;
    b = -a + c * d / e - f;

    p = q - r * s + t / -u;
    v = -p + w * x / y  - q * -r;
}