// Checks assignment statement with braces

int main(){
    int ans, a, b, c, d, e, f, g, h;
    read a;
    read b;
    read c;
    read d;
    read e;
    read f;
    read g;
    read h;
    ans = (((a+b) * (c-d)) / (-((e+f) * (g-h))));
    print ans;
}
