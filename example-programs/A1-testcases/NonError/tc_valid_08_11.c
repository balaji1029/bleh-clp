// Checks print statement and basic assignment statements with braces

void main(){
    int a, b, c;
    float p, x, y, z;

    read a;
    read b;
    read c;
    read x;
    read y;
    read z;

    p = (-a*-x + -a/-x + (-b*-y + -b/-y + (-c*-z + -c/-z)));

    print (p*p);

}